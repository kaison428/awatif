import { Node, Element } from "awatif-data-structure";

export function getArea(element: []): number {
    return 0;
};

export function getIxx(element: []): number {
    return 0;
};

export function getIyy(element: []): number {
    return 0;
};

export function getCentroid(element: []): number {
    return 0;
};